<!-- start banner Area -->
	<section class="banner-area">
		<div class="container">
			<div class="row fullscreen align-items-center justify-content-start">
				<div class="col-lg-12">
					<div class="active-banner-slider owl-carousel">
                    {% set count = 0 %}
                    {% for rs in slider %}
                        {% set count = count+1 %}
						<!-- single-slide -->
						<div class="row single-slide {% if count == 1 %} active {% endif %}">
							<div class="col-lg-5 col-sm-12 my-5">
								<div class="banner-content">
									<h1>{{rs.title}}</h1>
									<p>{{rs.description}}</p>
									<div class="add-bag d-flex align-items-center">
										<a class="add-btn" href="{{path('shopcart_new', {'productId':rs.id})}}"><span class="lnr lnr-cross"></span></a>
										<span class="add-text text-uppercase">Ajouter au panier</span>
									</div>
								</div>
							</div>
							<div class="col-lg-7 col-sm-12 my-5">
								<div class="banner-img">
								<a href="{{ path('product_show', {'id': rs.id}) }} ">
									<div class="scene">
										<div class="dvd-box">
											<div class="cover front" style="background-image: url('{{ asset('uploads/images/' ~ rs.image) | imagine_filter('slider_thumb') }}');">DVD Cover Front</div>
											<div class="cover back">DVD Cover Back</div>
											<div class="spine"></div>
										</div>
									</div>
                                </a>
                                    <!--<img class="img-fluid" src="{{ asset('uploads/images/' ~ rs.image) | imagine_filter('slider_thumb') }}" alt="{{rs.slug}}" style="height:384px;width: 657px;" >-->
                                
								</div>
							</div>
						</div>
                    {% endfor %}
					</div>
				</div>
			</div>
		</div>
	</section>
	<!-- End banner Area -->